[build]
buildCommand = "cd apps/dapp && npm install --legacy-peer-deps && npm run build"

[deploy]
startCommand = "cd apps/dapp && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
